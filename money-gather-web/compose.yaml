services:

  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules #Use docker's node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true #For hot reload